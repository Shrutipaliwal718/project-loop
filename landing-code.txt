
========== src\app\page.tsx ==========

import Navbar from "@/components/Common/Navbar";
import Footer from "@/components/Common/Footer";

import Hero from "@/components/Landing/Hero";
import Features from "@/components/Landing/Features";
import ProductOverview from "@/components/Landing/ProductOverview";
import HowItWorks from "@/components/Landing/HowItWorks";
import AIInsights from "@/components/Landing/AIInsights";
import AnalyticsPreview from "@/components/Landing/AnalyticsPreview";
import FAQ from "@/components/Landing/FAQ";
import CTA from "@/components/Landing/CTA";

export default function Home() {
  return (
    <main>
      <Navbar />

      <Hero />
      <Features />
      <ProductOverview />
      <HowItWorks />
      <AIInsights />
      <AnalyticsPreview />
      <FAQ />
      <CTA />

      <Footer />
    </main>
  );
}

========== src\app\globals.css ==========

@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --loop-bg: #030912;
  --loop-bg-soft: #07111d;
  --loop-surface: #091523;
  --loop-surface-2: #0d1b2b;

  --loop-text: #f8fafc;
  --loop-muted: #94a3b8;
  --loop-border: rgba(148, 163, 184, 0.16);

  --loop-cyan: #19e6d1;
  --loop-teal: #14d9c4;
  --loop-blue: #28a9ff;
  --loop-purple: #8b5cf6;
  --loop-violet: #c45cff;

  --loop-green: #35e879;
  --loop-amber: #f6a723;
  --loop-red: #ff4d72;
}

html {
  scroll-behavior: smooth;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background:
    radial-gradient(
      circle at 50% -10%,
      rgba(25, 230, 209, 0.08),
      transparent 35%
    ),
    radial-gradient(
      circle at 85% 35%,
      rgba(139, 92, 246, 0.08),
      transparent 30%
    ),
    var(--loop-bg);

  color: var(--loop-text);

  font-family: Arial, Helvetica, sans-serif;

  overflow-x: hidden;
}

::selection {
  background: rgba(25, 230, 209, 0.25);
  color: white;
}

/* =========================================================
   SCROLLBAR
   ========================================================= */

::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: #02070d;
}

::-webkit-scrollbar-thumb {
  background: rgba(25, 230, 209, 0.35);
  border-radius: 999px;
}

::-webkit-scrollbar-thumb:hover {
  background: rgba(25, 230, 209, 0.65);
}

/* =========================================================
   GLOBAL BACKGROUND
   ========================================================= */

.loop-page {
  position: relative;
  min-height: 100vh;
  background:
    radial-gradient(
      circle at 10% 15%,
      rgba(25, 230, 209, 0.055),
      transparent 25%
    ),
    radial-gradient(
      circle at 90% 25%,
      rgba(139, 92, 246, 0.06),
      transparent 25%
    );
}

.loop-grid {
  background-image:
    linear-gradient(rgba(255, 255, 255, 0.025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, 0.025) 1px, transparent 1px);

  background-size: 48px 48px;
}

/* =========================================================
   SECTION
   ========================================================= */

.loop-section {
  position: relative;
  padding: 110px 0;
}

.loop-container {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
}

/* =========================================================
   SECTION HEADING
   ========================================================= */

.loop-section-heading {
  text-align: center;
  max-width: 780px;
  margin: 0 auto 60px;
}

.loop-section-heading h2 {
  margin: 0;

  font-size: clamp(2rem, 4vw, 3rem);
  line-height: 1.1;

  font-weight: 800;
  letter-spacing: -0.04em;
}

.loop-section-heading p {
  margin: 18px auto 0;

  max-width: 680px;

  color: var(--loop-muted);

  font-size: 1rem;
  line-height: 1.8;
}

.loop-gradient-text {
  background: linear-gradient(90deg, #19e6d1, #28a9ff, #8b5cf6);

  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}

/* =========================================================
   CARDS
   ========================================================= */

.loop-card {
  position: relative;

  border: 1px solid var(--loop-border);

  border-radius: 22px;

  background:
    linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.055),
      rgba(255, 255, 255, 0.018)
    ),
    rgba(5, 15, 26, 0.86);

  backdrop-filter: blur(16px);

  transition:
    transform 350ms cubic-bezier(0.2, 0.8, 0.2, 1),
    border-color 350ms ease,
    box-shadow 350ms ease,
    background 350ms ease;
}

.loop-card:hover {
  transform: translateY(-8px);

  border-color: rgba(25, 230, 209, 0.4);

  box-shadow:
    0 20px 60px rgba(0, 0, 0, 0.32),
    0 0 35px rgba(25, 230, 209, 0.07);
}

/* =========================================================
   GLOW
   ========================================================= */

.loop-glow {
  box-shadow:
    0 0 30px rgba(25, 230, 209, 0.08),
    0 0 80px rgba(139, 92, 246, 0.04);
}

.loop-glow:hover {
  box-shadow:
    0 0 35px rgba(25, 230, 209, 0.16),
    0 0 100px rgba(139, 92, 246, 0.08);
}

/* =========================================================
   FLOAT ANIMATIONS
   ========================================================= */

@keyframes loop-float {
  0%,
  100% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-10px);
  }
}

@keyframes loop-float-slow {
  0%,
  100% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-16px);
  }
}

@keyframes loop-pulse {
  0%,
  100% {
    opacity: 0.55;
    transform: scale(1);
  }

  50% {
    opacity: 1;
    transform: scale(1.08);
  }
}

@keyframes loop-glow {
  0%,
  100% {
    box-shadow: 0 0 20px rgba(25, 230, 209, 0.04);
  }

  50% {
    box-shadow: 0 0 45px rgba(25, 230, 209, 0.16);
  }
}

@keyframes loop-scan {
  0% {
    transform: translateX(-120%);
  }

  100% {
    transform: translateX(120%);
  }
}

@keyframes loop-bar {
  0% {
    transform: scaleY(0.45);
  }

  50% {
    transform: scaleY(1);
  }

  100% {
    transform: scaleY(0.45);
  }
}

@keyframes loop-orbit {
  from {
    transform: rotate(0deg) translateX(8px) rotate(0deg);
  }

  to {
    transform: rotate(360deg) translateX(8px) rotate(-360deg);
  }
}

.loop-float {
  animation: loop-float 5s ease-in-out infinite;
}

.loop-float-slow {
  animation: loop-float-slow 7s ease-in-out infinite;
}

.loop-pulse {
  animation: loop-pulse 2.5s ease-in-out infinite;
}

.loop-glow-animation {
  animation: loop-glow 4s ease-in-out infinite;
}

.loop-scan {
  animation: loop-scan 4s linear infinite;
}

.loop-orbit {
  animation: loop-orbit 6s linear infinite;
}

.loop-bar-animation {
  transform-origin: bottom;
  animation: loop-bar 2.4s ease-in-out infinite;
}

/* =========================================================
   FLOATING BACKGROUND ORBS
   ========================================================= */

.loop-orb {
  position: absolute;

  width: 280px;
  height: 280px;

  border-radius: 999px;

  filter: blur(90px);

  pointer-events: none;
}

.loop-orb-cyan {
  background: rgba(25, 230, 209, 0.08);
}

.loop-orb-purple {
  background: rgba(139, 92, 246, 0.09);
}

/* =========================================================
   RESPONSIVE
   ========================================================= */

@media (max-width: 768px) {
  .loop-section {
    padding: 80px 0;
  }

  .loop-container {
    width: min(100% - 28px, 1180px);
  }

  .loop-section-heading {
    margin-bottom: 40px;
  }
}

/* =========================================================
   LIGHT MODE
   ========================================================= */

html.light {
  --loop-bg: #07111d;
  --loop-bg-soft: #0b1827;
  --loop-surface: #0e1d2e;
  --loop-surface-2: #13253a;

  --loop-text: #f8fafc;
  --loop-muted: #aab7c7;
  --loop-border: rgba(148, 163, 184, 0.2);
}

/*
  LOOP intentionally keeps a dark navy visual identity
  in light mode as requested.

  Light mode changes contrast and surface treatment rather
  than turning the entire product into a plain white page.
*/

html.light body {
  background: linear-gradient(135deg, #07111d 0%, #0c1928 55%, #101b32 100%);

  color: #f8fafc;
}

/* =========================================================
   REDUCED MOTION
   ========================================================= */

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
  }
}

========== src\app\layout.tsx ==========

import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "LOOP — Customer Feedback Intelligence",
  description:
    "AI-powered customer feedback intelligence platform for turning feedback into actionable insights.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

========== src\components\Common\Navbar.tsx ==========

"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import LoopLogo from "./LoopLogo";

export default function Navbar() {
  const [lightMode, setLightMode] = useState(false);
  const [menuOpen, setMenuOpen] = useState(false);

  useEffect(() => {
    document.documentElement.classList.toggle("light", lightMode);
  }, [lightMode]);

  const links = [
    { label: "Product", href: "#product" },
    { label: "Features", href: "#features" },
    { label: "How it works", href: "#how-it-works" },
    { label: "Insights", href: "#insights" },
  ];

  return (
    <header className="sticky top-0 z-50 border-b border-white/10 bg-[#030912]/85 backdrop-blur-2xl">
      <div className="mx-auto flex h-[76px] max-w-7xl items-center justify-between px-5 lg:px-8">
        <Link href="/" className="flex items-center gap-3">
          <LoopLogo size={45} />

          <div className="leading-none">
            <div className="text-xl font-extrabold tracking-tight text-white">
              LOOP
            </div>

            <div className="mt-1 text-[9px] font-semibold tracking-[0.18em] text-slate-400">
              FEEDBACK INTELLIGENCE
            </div>
          </div>
        </Link>

        <nav className="hidden items-center gap-9 md:flex">
          {links.map((link) => (
            <a
              key={link.href}
              href={link.href}
              className="relative text-sm font-medium text-slate-300 transition-colors duration-200 hover:text-white"
            >
              {link.label}

              <span className="absolute -bottom-2 left-0 h-px w-0 bg-gradient-to-r from-cyan-300 to-violet-400 transition-all duration-300 hover:w-full" />
            </a>
          ))}
        </nav>

        <div className="hidden items-center gap-3 md:flex">
          <button
            onClick={() => setLightMode((value) => !value)}
            className="flex h-10 w-10 items-center justify-center rounded-xl border border-white/10 bg-white/[0.03] text-slate-300 transition-all duration-300 hover:-translate-y-1 hover:border-cyan-300/30 hover:text-cyan-300"
            aria-label="Toggle theme"
          >
            {lightMode ? "☀" : "☾"}
          </button>

          <button className="px-4 py-2 text-sm font-semibold text-slate-300 transition hover:text-white">
            Sign in
          </button>

          <button className="rounded-xl bg-gradient-to-r from-cyan-300 to-teal-400 px-5 py-2.5 text-sm font-bold text-[#031017] shadow-[0_0_25px_rgba(25,230,209,0.15)] transition-all duration-300 hover:-translate-y-1 hover:shadow-[0_0_35px_rgba(25,230,209,0.28)]">
            Get started
          </button>
        </div>

        <button
          className="rounded-xl border border-white/10 px-3 py-2 text-slate-300 md:hidden"
          onClick={() => setMenuOpen((value) => !value)}
        >
          ☰
        </button>
      </div>

      {menuOpen && (
        <div className="border-t border-white/10 bg-[#050c15] px-5 py-5 md:hidden">
          <div className="flex flex-col gap-4">
            {links.map((link) => (
              <a
                key={link.href}
                href={link.href}
                onClick={() => setMenuOpen(false)}
                className="rounded-xl px-3 py-2 text-sm text-slate-300 transition hover:bg-white/5 hover:text-cyan-300"
              >
                {link.label}
              </a>
            ))}

            <button className="rounded-xl bg-cyan-300 px-4 py-3 font-bold text-[#031017]">
              Get started
            </button>
          </div>
        </div>
      )}
    </header>
  );
}

========== src\components\Common\Footer.tsx ==========

import Link from "next/link";
import LoopLogo from "./LoopLogo";

export default function Footer() {
  return (
    <footer className="border-t border-white/10 bg-[#02070d]">
      <div className="mx-auto max-w-7xl px-5 py-16 lg:px-8">
        <div className="grid gap-12 md:grid-cols-2 lg:grid-cols-[1.4fr_1fr_1fr_1.2fr]">
          <div>
            <Link href="/" className="flex items-center gap-3">
              <LoopLogo size={44} />

              <div>
                <div className="text-xl font-extrabold text-white">LOOP</div>

                <div className="mt-1 text-[9px] font-semibold tracking-[0.18em] text-slate-500">
                  FEEDBACK INTELLIGENCE
                </div>
              </div>
            </Link>

            <p className="mt-6 max-w-sm text-sm leading-7 text-slate-500">
              An AI-powered customer feedback intelligence platform designed to
              help teams understand sentiment, themes, trends and actions.
            </p>

            <div className="mt-6 flex items-center gap-2 text-xs text-slate-500">
              <span className="h-2 w-2 rounded-full bg-green-400" />
              Customer intelligence platform
            </div>
          </div>

          <FooterColumn
            title="Platform"
            links={[
              ["Product", "#product"],
              ["Features", "#features"],
              ["How it works", "#how-it-works"],
              ["Insights", "#insights"],
            ]}
          />

          <FooterColumn
            title="Intelligence"
            links={[
              ["Feedback Inbox", "/inbox"],
              ["Dashboard", "/dashboard"],
              ["Trends", "/trends"],
              ["Ask LOOP", "/ask"],
              ["Reports", "/reports"],
            ]}
          />

          <div>
            <h3 className="text-sm font-bold text-white">Contact</h3>

            <div className="mt-5 space-y-4 text-sm text-slate-500">
              <div>
                <div className="text-xs font-semibold text-slate-300">
                  Email
                </div>

                <div className="mt-1">hello@loop-feedback.example</div>
              </div>

              <div>
                <div className="text-xs font-semibold text-slate-300">
                  Location
                </div>

                <div className="mt-1">Bengaluru, India</div>
              </div>

              <div>
                <div className="text-xs font-semibold text-slate-300">
                  Availability
                </div>

                <div className="mt-1">Mon–Fri · 9:00 AM–6:00 PM</div>
              </div>
            </div>
          </div>
        </div>

        <div className="mt-14 flex flex-col gap-4 border-t border-white/10 pt-7 text-xs text-slate-600 sm:flex-row sm:items-center sm:justify-between">
          <div>© 2026 LOOP. Built for customer feedback intelligence.</div>

          <div className="flex gap-5">
            <span className="cursor-pointer transition hover:text-slate-300">
              Privacy
            </span>

            <span className="cursor-pointer transition hover:text-slate-300">
              Terms
            </span>

            <span className="cursor-pointer transition hover:text-slate-300">
              Security
            </span>
          </div>
        </div>
      </div>
    </footer>
  );
}

function FooterColumn({
  title,
  links,
}: {
  title: string;
  links: [string, string][];
}) {
  return (
    <div>
      <h3 className="text-sm font-bold text-white">{title}</h3>

      <div className="mt-5 space-y-3">
        {links.map(([label, href]) => (
          <Link
            key={label}
            href={href}
            className="block text-sm text-slate-500 transition-colors hover:text-cyan-300"
          >
            {label}
          </Link>
        ))}
      </div>
    </div>
  );
}

========== src\components\Common\LoopLogo.tsx ==========

type LoopLogoProps = {
  size?: number;
};

export default function LoopLogo({ size = 46 }: LoopLogoProps) {
  return (
    <div
      className="relative flex shrink-0 items-center justify-center"
      style={{
        width: size,
        height: size,
      }}
    >
      <div
        className="absolute inset-0 rounded-2xl opacity-30 blur-md"
        style={{
          background: "linear-gradient(135deg, #19e6d1, #28a9ff, #8b5cf6)",
        }}
      />

      <div
        className="relative flex h-full w-full items-center justify-center rounded-2xl border border-cyan-300/30 bg-[#071522]"
        style={{
          boxShadow: "0 0 24px rgba(25,230,209,.16)",
        }}
      >
        <svg
          width={size * 0.55}
          height={size * 0.55}
          viewBox="0 0 32 32"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <circle cx="16" cy="16" r="10" stroke="#19E6D1" strokeWidth="3" />

          <path
            d="M20.5 11.5C18.9 9.9 16.7 9 14.3 9C9.7 9 6 12.4 6 16.5C6 20.6 9.7 24 14.3 24C17.1 24 19.6 22.7 21.1 20.7"
            stroke="#28A9FF"
            strokeWidth="3"
            strokeLinecap="round"
          />

          <circle cx="21.5" cy="10.5" r="3" fill="#8B5CF6" />
        </svg>
      </div>
    </div>
  );
}

========== src\components\Landing\Hero.tsx ==========

export default function Hero() {
  return (
    <section className="relative overflow-hidden border-b border-white/[0.06]">
      <div className="loop-orb loop-orb-cyan left-[-120px] top-[220px]" />
      <div className="loop-orb loop-orb-purple right-[-100px] top-[250px]" />

      <div className="loop-grid absolute inset-0 opacity-40" />

      <div className="relative mx-auto max-w-7xl px-5 pb-20 pt-24 text-center lg:px-8 lg:pt-32">
        <div className="mx-auto mb-7 inline-flex items-center gap-2 rounded-full border border-cyan-300/40 bg-cyan-300/[0.05] px-4 py-2 text-sm font-semibold text-cyan-300 shadow-[0_0_30px_rgba(25,230,209,0.08)]">
          <span className="loop-pulse h-2 w-2 rounded-full bg-cyan-300" />
          AI-powered customer feedback intelligence
        </div>

        <h1 className="mx-auto max-w-5xl text-5xl font-extrabold leading-[1.02] tracking-[-0.055em] text-white sm:text-6xl lg:text-7xl">
          Your customers are talking.
          <br />
          <span className="loop-gradient-text">
            LOOP tells you what they mean.
          </span>
        </h1>

        <p className="mx-auto mt-7 max-w-3xl text-base leading-8 text-slate-400 sm:text-lg">
          LOOP transforms customer feedback into structured intelligence —
          revealing sentiment, recurring themes, emerging problems, feature
          requests, and the actions your team should prioritize next.
        </p>

        <div className="mt-9 flex flex-col items-center justify-center gap-4 sm:flex-row">
          <button className="group rounded-xl bg-gradient-to-r from-cyan-300 to-teal-400 px-7 py-3.5 font-bold text-[#031017] shadow-[0_0_35px_rgba(25,230,209,0.16)] transition-all duration-300 hover:-translate-y-1 hover:shadow-[0_0_45px_rgba(25,230,209,0.3)]">
            Explore LOOP
            <span className="ml-2 inline-block transition-transform group-hover:translate-x-1">
              →
            </span>
          </button>

          <button className="group flex items-center gap-3 rounded-xl border border-white/20 bg-white/[0.02] px-7 py-3.5 font-semibold text-white transition-all duration-300 hover:-translate-y-1 hover:border-cyan-300/40 hover:bg-white/[0.05]">
            <span className="flex h-7 w-7 items-center justify-center rounded-full border border-cyan-300/50 text-xs text-cyan-300">
              ▶
            </span>
            See how it works
          </button>
        </div>

        <div className="mx-auto mt-16 grid max-w-5xl grid-cols-2 gap-4 border-y border-white/[0.07] py-7 sm:grid-cols-4">
          {[
            ["✦", "AI-Powered Analysis"],
            ["◷", "Feedback Intelligence"],
            ["⌁", "Actionable Insights"],
            ["◇", "Team Decisions"],
          ].map(([icon, label]) => (
            <div
              key={label}
              className="flex items-center justify-center gap-3 text-sm text-slate-300"
            >
              <span className="text-xl text-cyan-300">{icon}</span>
              {label}
            </div>
          ))}
        </div>

        <div className="relative mx-auto mt-12 max-w-6xl">
          <div className="loop-card loop-glow overflow-hidden p-4 sm:p-6">
            <div className="mb-5 flex items-center justify-between border-b border-white/10 pb-4">
              <div className="text-left">
                <div className="text-[10px] font-bold tracking-[0.2em] text-cyan-300">
                  CUSTOMER INTELLIGENCE
                </div>

                <div className="mt-1 text-lg font-bold text-white">
                  Feedback overview
                </div>
              </div>

              <div className="flex items-center gap-2 rounded-full border border-cyan-300/20 bg-cyan-300/5 px-3 py-1.5 text-xs text-cyan-300">
                <span className="h-2 w-2 rounded-full bg-green-400" />
                Live intelligence
              </div>
            </div>

            <div className="grid gap-4 md:grid-cols-4">
              <HeroMetric
                icon="◌"
                label="Total feedback"
                value="12,480"
                change="+18.4%"
                type="cyan"
              />

              <HeroMetric
                icon="☺"
                label="Positive sentiment"
                value="72.6%"
                change="+6.2%"
                type="green"
              />

              <HeroMetric
                icon="◆"
                label="Themes detected"
                value="24"
                change="+4"
                type="purple"
              />

              <HeroMetric
                icon="ϟ"
                label="Action signals"
                value="17"
                change="8 urgent"
                type="amber"
              />
            </div>

            <div className="mt-4 grid gap-4 lg:grid-cols-[1.6fr_1fr]">
              <div className="rounded-2xl border border-white/10 bg-black/10 p-5">
                <div className="mb-5 flex items-center justify-between">
                  <div>
                    <div className="font-semibold text-white">
                      Sentiment movement
                    </div>
                    <div className="mt-1 text-xs text-slate-500">
                      Customer mood over the last 30 days
                    </div>
                  </div>

                  <span className="rounded-full bg-green-400/10 px-3 py-1 text-xs font-semibold text-green-400">
                    Improving ↗
                  </span>
                </div>

                <div className="flex h-36 items-end gap-2">
                  {[
                    30, 42, 37, 50, 45, 57, 52, 63, 68, 65, 72, 79, 76, 88, 91,
                    100,
                  ].map((height, index) => (
                    <div
                      key={index}
                      className="loop-bar-animation flex-1 rounded-t-md bg-gradient-to-t from-cyan-500/40 to-cyan-300"
                      style={{
                        height: `${height}%`,
                        animationDelay: `${index * 90}ms`,
                      }}
                    />
                  ))}
                </div>
              </div>

              <div className="rounded-2xl border border-white/10 bg-black/10 p-5">
                <div className="font-semibold text-white">
                  What customers feel
                </div>

                <div className="mt-5 space-y-4">
                  <ProgressRow
                    label="Positive"
                    value="72.6%"
                    width="72.6%"
                    className="bg-cyan-300"
                  />

                  <ProgressRow
                    label="Neutral"
                    value="18.1%"
                    width="18.1%"
                    className="bg-violet-400"
                  />

                  <ProgressRow
                    label="Negative"
                    value="9.3%"
                    width="9.3%"
                    className="bg-rose-400"
                  />
                </div>

                <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/5 p-3 text-left">
                  <div className="text-xs font-semibold text-cyan-300">
                    ✦ AI signal
                  </div>
                  <div className="mt-1 text-xs leading-5 text-slate-400">
                    Positive sentiment increased after recent product
                    improvements.
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div className="loop-float absolute -right-3 -top-8 hidden w-56 rounded-2xl border border-violet-400/30 bg-[#0b1726]/95 p-4 text-left shadow-[0_0_40px_rgba(139,92,246,0.12)] lg:block">
            <div className="text-xs font-bold text-violet-300">
              ✦ AI detected
            </div>

            <div className="mt-2 text-sm font-semibold text-white">
              New customer signal
            </div>

            <div className="mt-1 text-xs leading-5 text-slate-400">
              Delivery complaints are increasing this week.
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

function HeroMetric({
  icon,
  label,
  value,
  change,
  type,
}: {
  icon: string;
  label: string;
  value: string;
  change: string;
  type: "cyan" | "green" | "purple" | "amber";
}) {
  const styles = {
    cyan: "border-cyan-300/20 bg-cyan-300/[0.04] text-cyan-300",
    green: "border-green-400/20 bg-green-400/[0.04] text-green-400",
    purple: "border-violet-400/20 bg-violet-400/[0.04] text-violet-300",
    amber: "border-amber-400/20 bg-amber-400/[0.04] text-amber-300",
  };

  return (
    <div className={`loop-card rounded-2xl border p-4 ${styles[type]}`}>
      <div className="flex items-center gap-3">
        <div className="flex h-10 w-10 items-center justify-center rounded-xl border border-current/20 bg-black/20 text-lg">
          {icon}
        </div>

        <div>
          <div className="text-xs text-slate-400">{label}</div>

          <div className="mt-1 text-2xl font-extrabold text-white">{value}</div>
        </div>
      </div>

      <div className="mt-3 text-xs font-semibold">↑ {change}</div>
    </div>
  );
}

function ProgressRow({
  label,
  value,
  width,
  className,
}: {
  label: string;
  value: string;
  width: string;
  className: string;
}) {
  return (
    <div>
      <div className="mb-2 flex justify-between text-xs">
        <span className="text-slate-400">{label}</span>
        <span className="font-semibold text-white">{value}</span>
      </div>

      <div className="h-2 overflow-hidden rounded-full bg-white/5">
        <div className={`h-full rounded-full ${className}`} style={{ width }} />
      </div>
    </div>
  );
}

========== src\components\Landing\AnalyticsPreview.tsx ==========

const themes = [
  ["Delivery experience", "2,843", "88%"],
  ["Product quality", "1,827", "66%"],
  ["User interface", "1,492", "54%"],
  ["Feature requests", "1,201", "45%"],
  ["Customer support", "1,018", "38%"],
];

export default function AnalyticsPreview() {
  return (
    <section id="insights" className="loop-section overflow-hidden">
      <div className="loop-container">
        <div className="loop-section-heading">
          <div className="mb-4 text-xs font-bold tracking-[0.22em] text-cyan-300">
            CUSTOMER INTELLIGENCE DASHBOARD
          </div>

          <h2>
            See what your customers{" "}
            <span className="loop-gradient-text">actually mean.</span>
          </h2>

          <p>
            LOOP turns thousands of customer comments into visual signals your
            product team can understand at a glance.
          </p>
        </div>

        <div className="loop-card loop-glow overflow-hidden p-5 sm:p-7">
          <div className="mb-7 flex flex-col gap-4 border-b border-white/10 pb-6 sm:flex-row sm:items-center sm:justify-between">
            <div>
              <div className="text-[10px] font-bold tracking-[0.2em] text-cyan-300">
                CUSTOMER INTELLIGENCE
              </div>

              <h3 className="mt-2 text-2xl font-extrabold text-white">
                Feedback overview
              </h3>

              <p className="mt-1 text-sm text-slate-500">
                What changed in your customer voice this month
              </p>
            </div>

            <div className="flex items-center gap-2 self-start rounded-full border border-green-400/20 bg-green-400/5 px-4 py-2 text-xs font-semibold text-green-400">
              <span className="h-2 w-2 rounded-full bg-green-400" />
              Intelligence improving
            </div>
          </div>

          <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">
            <StatCard
              icon="◌"
              label="Total feedback"
              value="12,480"
              change="+18.4%"
              accent="cyan"
            />

            <StatCard
              icon="☺"
              label="Positive sentiment"
              value="72.6%"
              change="+6.2%"
              accent="green"
            />

            <StatCard
              icon="◆"
              label="Themes detected"
              value="24"
              change="+4 new"
              accent="purple"
            />

            <StatCard
              icon="ϟ"
              label="Action signals"
              value="17"
              change="8 urgent"
              accent="amber"
            />
          </div>

          <div className="mt-5 grid gap-5 xl:grid-cols-[1.55fr_0.95fr]">
            <div className="rounded-2xl border border-white/10 bg-black/10 p-5">
              <div className="mb-6 flex items-center justify-between">
                <div>
                  <h4 className="font-bold text-white">Sentiment movement</h4>

                  <p className="mt-1 text-xs text-slate-500">
                    Customer mood over the last 30 days
                  </p>
                </div>

                <span className="rounded-lg bg-cyan-300/10 px-3 py-1.5 text-xs font-semibold text-cyan-300">
                  ↗ Improving
                </span>
              </div>

              <div className="relative h-64">
                <div className="absolute inset-0 flex flex-col justify-between">
                  {[100, 75, 50, 25, 0].map((value) => (
                    <div key={value} className="flex items-center gap-3">
                      <span className="w-8 text-right text-[10px] text-slate-600">
                        {value}%
                      </span>

                      <div className="h-px flex-1 bg-white/[0.05]" />
                    </div>
                  ))}
                </div>

                <div className="absolute bottom-0 left-11 right-0 top-0 flex items-end gap-2 px-2">
                  {[
                    26, 33, 31, 40, 36, 43, 41, 47, 51, 49, 58, 62, 60, 72, 78,
                    84, 83, 96,
                  ].map((height, index) => (
                    <div
                      key={index}
                      className="group relative flex-1"
                      style={{ height: `${height}%` }}
                    >
                      <div className="absolute inset-0 rounded-t-lg bg-gradient-to-t from-cyan-500/30 to-cyan-300 transition-all duration-300 group-hover:from-cyan-400/60 group-hover:to-cyan-200" />
                    </div>
                  ))}
                </div>
              </div>

              <div className="mt-4 flex justify-between pl-11 text-[10px] text-slate-600">
                <span>30 days ago</span>
                <span>Today</span>
              </div>
            </div>

            <div className="rounded-2xl border border-white/10 bg-black/10 p-5">
              <h4 className="font-bold text-white">What customers feel</h4>

              <p className="mt-1 text-xs text-slate-500">
                Overall sentiment distribution
              </p>

              <div className="mt-7 space-y-6">
                <SentimentRow
                  label="Positive"
                  value="72.6%"
                  width="72.6%"
                  color="bg-cyan-300"
                />

                <SentimentRow
                  label="Neutral"
                  value="18.1%"
                  width="18.1%"
                  color="bg-violet-400"
                />

                <SentimentRow
                  label="Negative"
                  value="9.3%"
                  width="9.3%"
                  color="bg-rose-400"
                />
              </div>

              <div className="mt-7 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4">
                <div className="flex gap-3">
                  <span className="text-xl text-cyan-300">✦</span>

                  <div>
                    <div className="text-xs font-bold text-cyan-300">
                      AI signal
                    </div>

                    <p className="mt-1 text-xs leading-5 text-slate-400">
                      Positive sentiment increased after recent onboarding
                      improvements.
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div className="mt-5 grid gap-5 lg:grid-cols-3">
            <ThemeCard />

            <IssueCard />

            <ActionCard />
          </div>
        </div>
      </div>
    </section>
  );
}

function StatCard({
  icon,
  label,
  value,
  change,
  accent,
}: {
  icon: string;
  label: string;
  value: string;
  change: string;
  accent: string;
}) {
  const styles: Record<string, string> = {
    cyan: "text-cyan-300 border-cyan-300/20 bg-cyan-300/[0.04]",
    green: "text-green-400 border-green-400/20 bg-green-400/[0.04]",
    purple: "text-violet-300 border-violet-400/20 bg-violet-400/[0.04]",
    amber: "text-amber-300 border-amber-400/20 bg-amber-400/[0.04]",
  };

  return (
    <div className={`loop-card p-5 ${styles[accent]}`}>
      <div className="flex items-center gap-3">
        <div className="flex h-11 w-11 items-center justify-center rounded-xl border border-current/20 bg-black/20 text-xl">
          {icon}
        </div>

        <span className="text-xs text-slate-400">{label}</span>
      </div>

      <div className="mt-5 text-3xl font-extrabold text-white">{value}</div>

      <div className="mt-2 text-xs font-semibold">↑ {change}</div>
    </div>
  );
}

function SentimentRow({
  label,
  value,
  width,
  color,
}: {
  label: string;
  value: string;
  width: string;
  color: string;
}) {
  return (
    <div>
      <div className="mb-2 flex justify-between text-sm">
        <span className="text-slate-300">{label}</span>
        <span className="font-bold text-white">{value}</span>
      </div>

      <div className="h-2 overflow-hidden rounded-full bg-white/5">
        <div className={`h-full rounded-full ${color}`} style={{ width }} />
      </div>
    </div>
  );
}

function ThemeCard() {
  return (
    <div className="loop-card p-5">
      <div className="flex items-center justify-between">
        <div>
          <h4 className="font-bold text-white">Top themes</h4>

          <p className="mt-1 text-xs text-slate-500">
            Themes mentioned by customers
          </p>
        </div>

        <span className="text-xs font-semibold text-cyan-300">View all</span>
      </div>

      <div className="mt-6 space-y-4">
        {themes.map(([name, count, width]) => (
          <div key={name}>
            <div className="mb-2 flex items-center justify-between gap-3">
              <span className="text-xs text-slate-300">{name}</span>

              <span className="text-xs font-semibold text-slate-400">
                {count}
              </span>
            </div>

            <div className="h-1.5 rounded-full bg-white/5">
              <div
                className="h-full rounded-full bg-cyan-300"
                style={{ width }}
              />
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

function IssueCard() {
  return (
    <div className="loop-card p-5">
      <div className="flex items-center justify-between">
        <div>
          <h4 className="font-bold text-white">Emerging issue</h4>

          <p className="mt-1 text-xs text-slate-500">
            New issues gaining attention
          </p>
        </div>

        <span className="text-xs font-semibold text-cyan-300">View all</span>
      </div>

      <div className="mt-6 rounded-xl border border-rose-400/20 bg-rose-400/[0.04] p-4">
        <div className="flex items-start gap-3">
          <div className="flex h-10 w-10 items-center justify-center rounded-xl bg-rose-400/10 text-lg text-rose-400">
            !
          </div>

          <div className="flex-1">
            <div className="font-semibold text-white">
              App performance on low-end devices
            </div>

            <div className="mt-2 text-xs font-semibold text-green-400">
              ↑ 32% vs last 7 days
            </div>
          </div>
        </div>

        <p className="mt-4 text-xs leading-5 text-slate-400">
          Customers are reporting crashes and slow loading on budget devices.
        </p>

        <button className="mt-4 rounded-lg border border-cyan-300/30 px-3 py-2 text-xs font-semibold text-cyan-300 transition hover:bg-cyan-300/10">
          View feedback
        </button>
      </div>
    </div>
  );
}

function ActionCard() {
  const actions = [
    ["▣", "Late deliveries", "23 mentions", "Urgent"],
    ["▤", "Payment failed", "18 mentions", "Urgent"],
    ["△", "App crashes", "15 mentions", "Urgent"],
    ["◐", "Dark mode request", "12 mentions", "High"],
  ];

  return (
    <div className="loop-card p-5">
      <div className="flex items-center justify-between">
        <div>
          <h4 className="font-bold text-white">Recent action signals</h4>

          <p className="mt-1 text-xs text-slate-500">High-priority feedback</p>
        </div>

        <span className="text-xs font-semibold text-cyan-300">View all</span>
      </div>

      <div className="mt-5 space-y-3">
        {actions.map(([icon, title, count, priority]) => (
          <div
            key={title}
            className="flex items-center gap-3 rounded-xl border border-white/[0.06] bg-white/[0.02] p-3"
          >
            <div className="flex h-9 w-9 items-center justify-center rounded-lg bg-cyan-300/10 text-cyan-300">
              {icon}
            </div>

            <div className="min-w-0 flex-1">
              <div className="truncate text-xs font-semibold text-slate-200">
                {title}
              </div>

              <div className="mt-1 text-[10px] text-slate-500">{count}</div>
            </div>

            <span className="rounded-md bg-rose-400/10 px-2 py-1 text-[9px] font-bold text-rose-400">
              {priority}
            </span>
          </div>
        ))}
      </div>
    </div>
  );
}

========== src\components\Landing\ProductOverview.tsx ==========

export default function ProductOverview() {
  return (
    <section id="product" className="loop-section">
      <div className="loop-container">
        <div className="loop-section-heading">
          <div className="mb-4 text-xs font-bold tracking-[0.22em] text-cyan-300">
            THE LOOP INTELLIGENCE LAYER
          </div>

          <h2>
            From raw feedback to{" "}
            <span className="loop-gradient-text">decisions.</span>
          </h2>

          <p>
            Customer feedback is valuable, but thousands of comments are
            difficult to interpret manually. LOOP organizes that voice into
            patterns your team can understand and act on.
          </p>
        </div>

        <div className="grid gap-5 md:grid-cols-3">
          <OverviewCard
            icon="◌"
            title="Collect"
            description="Bring customer feedback into one structured workspace through manual input, CSV uploads, and simulated feedback channels."
            accent="cyan"
          />

          <OverviewCard
            icon="✦"
            title="Understand"
            description="AI analyzes feedback to identify sentiment, categories, recurring themes, emerging issues, and feature requests."
            accent="purple"
          />

          <OverviewCard
            icon="ϟ"
            title="Act"
            description="Turn customer signals into prioritized insights, reports, and questions your team can use for better product decisions."
            accent="amber"
          />
        </div>
      </div>
    </section>
  );
}

function OverviewCard({
  icon,
  title,
  description,
  accent,
}: {
  icon: string;
  title: string;
  description: string;
  accent: "cyan" | "purple" | "amber";
}) {
  const colors = {
    cyan: "text-cyan-300 border-cyan-300/20 bg-cyan-300/5",
    purple: "text-violet-300 border-violet-400/20 bg-violet-400/5",
    amber: "text-amber-300 border-amber-400/20 bg-amber-400/5",
  };

  return (
    <div className="loop-card p-7">
      <div
        className={`mb-6 flex h-14 w-14 items-center justify-center rounded-2xl border text-2xl ${colors[accent]}`}
      >
        {icon}
      </div>

      <div className="text-xl font-bold text-white">{title}</div>

      <p className="mt-3 text-sm leading-7 text-slate-400">{description}</p>

      <div className="mt-6 h-px bg-gradient-to-r from-white/10 to-transparent" />

      <div className="mt-5 text-xs font-semibold text-slate-500">
        LOOP intelligence layer →
      </div>
    </div>
  );
}

========== src\components\Landing\Features.tsx ==========

const features = [
  {
    icon: "◌",
    title: "Feedback Inbox",
    text: "Search, filter and review customer feedback from a centralized workspace instead of working across disconnected comments.",
    points: ["Searchable feedback", "Status filtering", "Pagination"],
    color: "cyan",
  },
  {
    icon: "☺",
    title: "Sentiment Analysis",
    text: "Automatically understand whether customer feedback is positive, neutral or negative and monitor how sentiment changes.",
    points: [
      "Positive / neutral / negative",
      "Sentiment trends",
      "Change detection",
    ],
    color: "green",
  },
  {
    icon: "◆",
    title: "Theme Detection",
    text: "Discover recurring topics across thousands of comments and understand which themes customers mention most frequently.",
    points: ["Theme clustering", "Mention frequency", "Recurring patterns"],
    color: "purple",
  },
  {
    icon: "⌁",
    title: "Emerging Trends",
    text: "Surface new issues and changing customer concerns before they become difficult-to-ignore product problems.",
    points: ["Trend movement", "Emerging issues", "Time-based analysis"],
    color: "blue",
  },
  {
    icon: "ϟ",
    title: "Action Signals",
    text: "Highlight feedback that deserves attention so teams can distinguish important signals from ordinary customer noise.",
    points: ["Priority indicators", "Urgent feedback", "Action opportunities"],
    color: "amber",
  },
  {
    icon: "✦",
    title: "Ask LOOP",
    text: "Ask natural-language questions about your customer feedback and receive intelligence based on the available feedback data.",
    points: [
      "Natural-language Q&A",
      "Feedback-aware answers",
      "Decision support",
    ],
    color: "violet",
  },
];

export default function Features() {
  return (
    <section id="features" className="loop-section">
      <div className="loop-container">
        <div className="loop-section-heading">
          <div className="mb-4 text-xs font-bold tracking-[0.22em] text-cyan-300">
            BUILT FOR CUSTOMER VOICE
          </div>

          <h2>
            Intelligence for every{" "}
            <span className="loop-gradient-text">feedback signal.</span>
          </h2>

          <p>
            LOOP connects feedback exploration, AI analysis, analytics and
            decision support into one customer-intelligence workflow.
          </p>
        </div>

        <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">
          {features.map((feature, index) => (
            <FeatureCard key={feature.title} feature={feature} index={index} />
          ))}
        </div>
      </div>
    </section>
  );
}

function FeatureCard({
  feature,
  index,
}: {
  feature: (typeof features)[number];
  index: number;
}) {
  const colorMap = {
    cyan: {
      icon: "text-cyan-300 border-cyan-300/20 bg-cyan-300/5",
      line: "from-cyan-300",
    },
    green: {
      icon: "text-green-400 border-green-400/20 bg-green-400/5",
      line: "from-green-400",
    },
    purple: {
      icon: "text-violet-300 border-violet-400/20 bg-violet-400/5",
      line: "from-violet-400",
    },
    blue: {
      icon: "text-blue-300 border-blue-400/20 bg-blue-400/5",
      line: "from-blue-400",
    },
    amber: {
      icon: "text-amber-300 border-amber-400/20 bg-amber-400/5",
      line: "from-amber-400",
    },
    violet: {
      icon: "text-fuchsia-300 border-fuchsia-400/20 bg-fuchsia-400/5",
      line: "from-fuchsia-400",
    },
  };

  const colors = colorMap[feature.color as keyof typeof colorMap];

  return (
    <div
      className="loop-card group overflow-hidden p-7"
      style={{
        animationDelay: `${index * 100}ms`,
      }}
    >
      <div
        className={`mb-6 flex h-14 w-14 items-center justify-center rounded-2xl border text-2xl transition-transform duration-300 group-hover:scale-110 group-hover:rotate-3 ${colors.icon}`}
      >
        {feature.icon}
      </div>

      <h3 className="text-xl font-bold text-white">{feature.title}</h3>

      <p className="mt-3 text-sm leading-7 text-slate-400">{feature.text}</p>

      <div className="mt-6 space-y-3">
        {feature.points.map((point) => (
          <div
            key={point}
            className="flex items-center gap-3 text-xs text-slate-300"
          >
            <span
              className={`h-1.5 w-1.5 rounded-full bg-gradient-to-r ${colors.line} to-transparent`}
            />

            {point}
          </div>
        ))}
      </div>

      <div className="mt-7 h-px bg-white/10" />

      <div className="mt-5 text-xs font-semibold text-slate-500 transition-colors group-hover:text-cyan-300">
        Explore capability →
      </div>
    </div>
  );
}

========== src\components\Landing\HowItWorks.tsx ==========

const steps = [
  {
    number: "01",
    icon: "↓",
    title: "Bring customer feedback into LOOP",
    description:
      "Add feedback manually, upload structured CSV data, or use the simulated feedback channel supported by the project.",
    detail: "Feedback ingestion",
  },
  {
    number: "02",
    icon: "✦",
    title: "LOOP analyzes the customer voice",
    description:
      "AI processes feedback to identify sentiment, categories, recurring themes, emerging issues and meaningful customer signals.",
    detail: "AI classification",
  },
  {
    number: "03",
    icon: "◈",
    title: "Explore patterns and trends",
    description:
      "Use the analytics dashboard to understand sentiment movement, theme frequency, trends and high-priority feedback.",
    detail: "Customer intelligence",
  },
  {
    number: "04",
    icon: "ϟ",
    title: "Turn intelligence into action",
    description:
      "Use Ask LOOP and Voice-of-Customer reporting to answer questions and communicate what your customers are telling your team.",
    detail: "Decision support",
  },
];

export default function HowItWorks() {
  return (
    <section id="how-it-works" className="loop-section">
      <div className="loop-container">
        <div className="loop-section-heading">
          <div className="mb-4 text-xs font-bold tracking-[0.22em] text-cyan-300">
            HOW LOOP WORKS
          </div>

          <h2>
            From feedback to{" "}
            <span className="loop-gradient-text">customer intelligence.</span>
          </h2>

          <p>
            LOOP creates a simple path from raw customer comments to structured
            intelligence and product decisions.
          </p>
        </div>

        <div className="mx-auto max-w-4xl space-y-5">
          {steps.map((step, index) => (
            <div
              key={step.number}
              className="loop-card group relative flex flex-col gap-6 p-6 sm:flex-row sm:items-center sm:p-7"
            >
              <div className="flex items-center gap-5">
                <div className="flex h-16 w-16 shrink-0 items-center justify-center rounded-2xl border border-cyan-300/20 bg-cyan-300/5 text-xl font-bold text-cyan-300 transition-all duration-300 group-hover:scale-105 group-hover:border-cyan-300/50">
                  {step.icon}
                </div>

                <div className="sm:hidden">
                  <div className="text-xs font-bold tracking-widest text-slate-600">
                    STEP {step.number}
                  </div>

                  <h3 className="mt-1 text-lg font-bold text-white">
                    {step.title}
                  </h3>
                </div>
              </div>

              <div className="hidden min-w-[70px] sm:block">
                <div className="text-xs font-bold tracking-widest text-cyan-300">
                  STEP
                </div>

                <div className="mt-1 text-2xl font-extrabold text-white">
                  {step.number}
                </div>
              </div>

              <div className="flex-1">
                <h3 className="hidden text-xl font-bold text-white sm:block">
                  {step.title}
                </h3>

                <p className="mt-2 text-sm leading-7 text-slate-400">
                  {step.description}
                </p>
              </div>

              <div className="rounded-full border border-white/10 bg-white/[0.025] px-4 py-2 text-xs font-semibold text-slate-400">
                {step.detail}
              </div>

              {index !== steps.length - 1 && (
                <div className="absolute -bottom-6 left-1/2 hidden h-7 w-px bg-gradient-to-b from-cyan-300/30 to-transparent sm:block" />
              )}
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

========== src\components\Landing\AIInsights.tsx ==========

export default function AIInsights() {
  return (
    <section className="loop-section">
      <div className="loop-container">
        <div className="loop-section-heading">
          <div className="mb-4 text-xs font-bold tracking-[0.22em] text-violet-300">
            AI-POWERED INTELLIGENCE
          </div>

          <h2>
            Don't just collect feedback.
            <br />
            <span className="loop-gradient-text">Understand it.</span>
          </h2>

          <p>
            LOOP helps your team move beyond individual comments by connecting
            sentiment, themes, trends and action signals into one intelligence
            layer.
          </p>
        </div>

        <div className="grid gap-5 lg:grid-cols-3">
          <InsightCard
            icon="✦"
            title="AI classification"
            description="Automatically classify customer feedback into meaningful categories and sentiment signals."
            metric="3"
            metricLabel="sentiment classes"
            accent="cyan"
          />

          <InsightCard
            icon="◆"
            title="Theme intelligence"
            description="Group related feedback into recurring themes so teams can see what customers repeatedly talk about."
            metric="24"
            metricLabel="themes detected"
            accent="purple"
          />

          <InsightCard
            icon="ϟ"
            title="Action signals"
            description="Surface important feedback and emerging problems that may require product or customer-experience attention."
            metric="17"
            metricLabel="action signals"
            accent="amber"
          />
        </div>
      </div>
    </section>
  );
}

function InsightCard({
  icon,
  title,
  description,
  metric,
  metricLabel,
  accent,
}: {
  icon: string;
  title: string;
  description: string;
  metric: string;
  metricLabel: string;
  accent: "cyan" | "purple" | "amber";
}) {
  const styles = {
    cyan: "text-cyan-300 border-cyan-300/20 bg-cyan-300/[0.04]",
    purple: "text-violet-300 border-violet-400/20 bg-violet-400/[0.04]",
    amber: "text-amber-300 border-amber-400/20 bg-amber-400/[0.04]",
  };

  return (
    <div className="loop-card loop-float-slow p-7">
      <div
        className={`flex h-14 w-14 items-center justify-center rounded-2xl border text-2xl ${styles[accent]}`}
      >
        {icon}
      </div>

      <h3 className="mt-7 text-xl font-bold text-white">{title}</h3>

      <p className="mt-3 text-sm leading-7 text-slate-400">{description}</p>

      <div className="mt-7 flex items-end justify-between border-t border-white/10 pt-5">
        <div>
          <div className="text-3xl font-extrabold text-white">{metric}</div>

          <div className="mt-1 text-xs text-slate-500">{metricLabel}</div>
        </div>

        <div className="text-2xl opacity-60">↗</div>
      </div>
    </div>
  );
}

========== src\components\Landing\FAQ.tsx ==========

export default function AIInsights() {
  return (
    <section className="loop-section">
      <div className="loop-container">
        <div className="loop-section-heading">
          <div className="mb-4 text-xs font-bold tracking-[0.22em] text-violet-300">
            AI-POWERED INTELLIGENCE
          </div>

          <h2>
            Don't just collect feedback.
            <br />
            <span className="loop-gradient-text">Understand it.</span>
          </h2>

          <p>
            LOOP helps your team move beyond individual comments by connecting
            sentiment, themes, trends and action signals into one intelligence
            layer.
          </p>
        </div>

        <div className="grid gap-5 lg:grid-cols-3">
          <InsightCard
            icon="✦"
            title="AI classification"
            description="Automatically classify customer feedback into meaningful categories and sentiment signals."
            metric="3"
            metricLabel="sentiment classes"
            accent="cyan"
          />

          <InsightCard
            icon="◆"
            title="Theme intelligence"
            description="Group related feedback into recurring themes so teams can see what customers repeatedly talk about."
            metric="24"
            metricLabel="themes detected"
            accent="purple"
          />

          <InsightCard
            icon="ϟ"
            title="Action signals"
            description="Surface important feedback and emerging problems that may require product or customer-experience attention."
            metric="17"
            metricLabel="action signals"
            accent="amber"
          />
        </div>
      </div>
    </section>
  );
}

function InsightCard({
  icon,
  title,
  description,
  metric,
  metricLabel,
  accent,
}: {
  icon: string;
  title: string;
  description: string;
  metric: string;
  metricLabel: string;
  accent: "cyan" | "purple" | "amber";
}) {
  const styles = {
    cyan: "text-cyan-300 border-cyan-300/20 bg-cyan-300/[0.04]",
    purple: "text-violet-300 border-violet-400/20 bg-violet-400/[0.04]",
    amber: "text-amber-300 border-amber-400/20 bg-amber-400/[0.04]",
  };

  return (
    <div className="loop-card loop-float-slow p-7">
      <div
        className={`flex h-14 w-14 items-center justify-center rounded-2xl border text-2xl ${styles[accent]}`}
      >
        {icon}
      </div>

      <h3 className="mt-7 text-xl font-bold text-white">{title}</h3>

      <p className="mt-3 text-sm leading-7 text-slate-400">{description}</p>

      <div className="mt-7 flex items-end justify-between border-t border-white/10 pt-5">
        <div>
          <div className="text-3xl font-extrabold text-white">{metric}</div>

          <div className="mt-1 text-xs text-slate-500">{metricLabel}</div>
        </div>

        <div className="text-2xl opacity-60">↗</div>
      </div>
    </div>
  );
}

========== src\components\Landing\CTA.tsx ==========

import LoopLogo from "../Common/LoopLogo";

export default function CTA() {
  return (
    <section className="loop-section pt-20">
      <div className="loop-container">
        <div className="relative overflow-hidden rounded-3xl border border-cyan-300/20 bg-gradient-to-br from-cyan-300/[0.08] via-[#091523] to-violet-500/[0.08] p-8 text-center shadow-[0_0_80px_rgba(25,230,209,0.05)] sm:p-14">
          <div className="absolute -left-24 -top-24 h-56 w-56 rounded-full bg-cyan-300/10 blur-3xl" />
          <div className="absolute -bottom-24 -right-24 h-56 w-56 rounded-full bg-violet-500/10 blur-3xl" />

          <div className="relative">
            <div className="mx-auto mb-6 flex w-fit">
              <LoopLogo size={54} />
            </div>

            <div className="text-xs font-bold tracking-[0.22em] text-cyan-300">
              CLOSE THE LOOP
            </div>

            <h2 className="mx-auto mt-4 max-w-3xl text-3xl font-extrabold tracking-tight text-white sm:text-5xl">
              Turn customer feedback into your next best decision.
            </h2>

            <p className="mx-auto mt-5 max-w-2xl text-sm leading-7 text-slate-400 sm:text-base">
              Explore how LOOP connects customer feedback, AI analysis,
              analytics and actionable intelligence in one workspace.
            </p>

            <button className="mt-8 rounded-xl bg-gradient-to-r from-cyan-300 to-teal-400 px-7 py-3.5 font-bold text-[#031017] shadow-[0_0_35px_rgba(25,230,209,0.15)] transition-all duration-300 hover:-translate-y-1 hover:shadow-[0_0_45px_rgba(25,230,209,0.28)]">
              Get started with LOOP →
            </button>
          </div>
        </div>
      </div>
    </section>
  );
}
